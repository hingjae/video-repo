extends base
block contents
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(type="text" placeholder="title" required name="title")
        input(type="text" placeholder="contents" required name="contents")
        input(type="text" placeholder="hashtags" name="hashtags")
        input(type="submit" value="post")